<div class="col-md-8">
  <table class="table">
    <tr>
      <th>Artists</th>
    </tr>
    <% @artists.each do |artist| %>
      <tr>
        <td>
          <a href="/artists/<%= artist.slug %>"><%= artist.name %></a>
        </td>
      </tr>
    <% end %>
  </table>
</div>